# 修改过程记录

本文件记录 Reader（PyReader）项目近期的重构与问题修复过程。

## 一、单体 reader.py 重构为 pyreader 包

### 背景

`reader.py` 原本是一个 3715 行的单体文件，配置、分页、Markdown 渲染、PDF 解析、
AI 翻译、语音朗读、视图绘制、主窗口逻辑全部堆在一起，难以维护。

### 改动

- `reader.py` 保留为 10 行入口，改为 `from pyreader import MainWindow`
- 按功能拆分为 12 个模块：
  `config` / `model` / `markdown` / `textio` / `pager` / `pdf` / `ai` /
  `tts` / `loader` / `view` / `settings` / `mainwin`
- 清理 `settings.py` 中重复定义的 `AI_PRESETS`（复用 `config.py`）
- `.gitignore` 增加忽略打包产物 `build/`、`dist/`、`*.zip`

## 二、重构后 NameError 回归修复

### 现象

朗读时报 `NameError: name 'TTS_DEFAULT_VOICE' is not defined`。

### 定位

用 `symtable` 对全部 13 个模块做未定义名静态扫描，发现拆分时共遗漏了 11 处
跨模块引用的 import：

- `mainwin.py`：`TTS_DEFAULT_VOICE`、`_log_tts`、`Optional`
- `pdf.py`：`MD_BOLD`、`MD_ITALIC`、`MD_CODE`、`MD_H_GAP`、`MD_LIST_INDENT`、
  `_wrap_pieces`、`_line_from_pieces`、`load_epub`、`load_html_file`

这些名字在旧单体文件里是全局变量/函数，拆分后需要显式从各自模块导入。

### 修复

补全上述 import（`mainwin.py` 从 `.tts` 导入 TTS 常量与日志函数、加 `typing.Optional`；
`pdf.py` 从 `.markdown` / `.textio` 导入缺失的渲染函数与常量）。

## 三、朗读功能不工作

### 现象

点击「朗读」后合成失败，`tts.log` 里反复出现
`No audio was received. Please verify that your parameters are correct.`，
整个朗读随即中断。

### 定位

1. **音色与文本语言不匹配**：`config.json` 里 `tts_voice` 被设置成英文男声
   `en-GB-RyanNeural`，而读的是中文书 → edge-tts 无法用英文音色合成中文，
   返回 "No audio"。
2. **失败处理脆弱**：旧代码里任何一句合成失败都会 `_tts_stop()`，直接中断整章，
   一个标点句 / 短句就能让朗读停下来。

### 修复

1. 把默认音色改回中文 `zh-CN-XiaoxiaoNeural`。
2. 单句合成失败改为「跳过该句继续读下一句」：
   - `TtsWorker.failed` 信号改为携带句子序号 `(seq, msg)`
   - 新增 `_tts_on_failed(epoch, seq, msg)`，把失败句记入 `tts_failed_seqs`
   - `_tts_advance()` 自动跳过已失败句子，整章读完后才停止

## 四、翻页动画「字体变大」

### 现象

翻页动画过程中，翻动页上的文字明显变大。

### 定位

翻动页自由边高度用透视公式 `hs = H * D / den` 计算，翻页时自由边「抬向镜头」，
高度最多被放大到 `D/(D-W) = 1.25` 倍（t≈0.3 时约 1.19 倍），文字随之被拉大。

### 修复

自由边高度改为恒定 `hs = H`（绕垂直书脊旋转时页面高度本就不变，只有宽度收窄）；
透视仍保留在水平摆动方向，翻页的 3D 动感还在，但不再有「大字」。
